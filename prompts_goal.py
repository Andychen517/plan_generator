# -*- coding: utf-8 -*-
"""goal_gen 的静态提示词。注:按 WITH_ORIGIN/WITH_QUANT 开关条件拼装的模板不在本文件——A 步的随站在 step_a.py,C/D 步的留在 goal_gen.py 里。"""

B_SYS = (
    "你是资深科研策略专家,擅长横向对照多个方法、发现研究空白。"
)

B_USER_HEAD = """下面是已结构化的方法单元(JSON):
"""

B_USER_TAIL = """

研究需求/已给假设(抽 gap 时优先贴合这个方向):{req}

任务:横向对照这些单元,找出隐含的研究空白(gap)。
要求:
1. 产出 1 到 {n} 个 gap(至多 {n} 个),宁缺毋滥:只保留有真实依据、彼此实质不同的空白;
   若现状可提升空间有限,少给甚至只给 1 个也可以,绝不为凑数而拆分、换词重述或牵强编造。
   在满足以上前提下,尽量覆盖多种类型:
   - 能力缺口:没有任何方法同时做到 X 和 Y
   - 数据场景缺口:大家只在某类数据/语言/规模上验证
   - 假设失效:某个被默认的前提在某情况下不成立
   - 结论矛盾:两个单元结论打架、无人解释
   - 迁移缺口:别的领域成熟做法这里还没人试
2. 每个 gap 必须引用它涉及的单元 id(至少一个),并说清具体张力。
3. 各 gap 必须实质不同,不能是同一空白的换词重述;类型也尽量不重复。

严格输出一个 JSON 数组,每个元素字段:
- gap_id:         如 "G1"
- type:           上述五类之一
- summary:        一句话说清这个空白
- involved_units: 涉及的单元 id 列表,如 ["U1","U3"]
- tension:        为什么这是个空白(具体张力)
只输出 JSON,不要任何额外文字。
"""

C_USER_HEAD = """研究空白(JSON):
"""

C_USER_REQ = """

研究需求/已给假设(目标要贴合这个方向):"""

D_SYS = (
    "你是国家级项目申报书的撰写专家,擅长把一条研究方向写成一段完整、连贯、有说服力的"
    "『研究目标』正文。语言明确、具体、不空泛,忠于给定材料,绝不编造数字,绝不出现内部编号,"
    "绝不使用未经定义的数学指标或符号。"
)

DEDUP_SYS = (
    "你是严格的科研空白审查员。判断哪些研究空白(gap)是同一个空白的不同说法,把它们分到一组;"
    "不同的空白保持独立。只依据给定材料,不臆造。"
)

DEDUP_HEAD = "下面是抽出的研究空白 gap 列表(JSON):\n"

DEDUP_TAIL = """

判据:两个 gap 指向同一个底层局限/张力、会推导出实质相同的研究目标 → 算重复,合并到一组;
只要对应不同局限、会长出不同目标 → 不同,各自成组。宁可少合,不要把不同的空白硬凑到一起。

把上面所有 gap 分组,为每组输出:
- merged_from:该组包含的原 gap_id 列表(如 ["G1","G4"];没重复的就只有一个)
- type:该组的 gap 类型
- summary:一条综合 summary,完整涵盖该组所有 gap 的空白含义、措辞清楚
         (可直接采用最清楚的一条,也可综合改写)
- tension:综合后的具体张力

严格输出一个 JSON 数组,每个元素含 merged_from / type / summary / tension。
不要输出 involved_units(证据单元由程序按 merged_from 自动合并)。
只输出 JSON,不要任何额外文字。
"""

REFLECT_SYS = (
    "你是严格的科研选题审稿人。对一条候选研究目标做质量审查,只依据给定材料判断,"
    "不放水、不脑补。逐条按标准打分,发现硬伤就判淘汰。"
)

TOURNEY_SYS = (
    "你是项目评审委员会。对若干条已通过质量审查的候选研究目标,按依据强度、研究价值、可行性"
    "打分,给它们排出高低。不放水,只依据给定材料。"
)

TOURNEY_HEAD = "下面是若干条研究目标(含正文):\n"

TOURNEY_TAIL = """

请给每条候选按以下 3 个维度打分(各 1-5 分)并给一句理由:
1. 依据强度:被综述证据支撑的充分度——引用单元的数量、相关性、支撑力度。
   (5=大量强相关证据直接支撑;3=有一些相关证据;1=证据稀少或勉强相关)
2. 研究价值:填这个空白的重要性与影响。
   (5=核心痛点、填了影响大;3=有价值但非核心;1=边缘、影响小)
3. 可行性:该目标可做、可落地的程度。
   (5=路径清晰、范围合理、可操作;3=可做但有难度;1=太笼统或极难实现)

严格输出一个 JSON 数组,每个元素字段:
- gap_id:候选的 gap_id
- scores:对象,含 evidence / significance / feasibility,各 1-5 整数
- reason:一句话打分依据
只输出 JSON,不要任何额外文字。
"""

TOY_REVIEW = """科学文献问答的现状:方法 RAG-A(美国某团队)在 ScholarQABench 英文子集上引用
F1 达 61.2%,但只在英文验证,且依赖闭源检索,不可复现。方法 RAG-B(国内某团队)
在中文科学问答上引用 F1 约 55%,覆盖中英双语,但推理延迟高达 3s/问,难以实用。
方法 Iter-C 引入迭代反思,在英文上把引用 F1 提到 68%,但需要 3 轮调用、成本高,
且未在中文验证。三者都假设检索库已覆盖所需文献。"""

